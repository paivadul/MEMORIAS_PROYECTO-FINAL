import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFF7EB',
    },
    secondary: {
      main: '#BFBBB8',
    },
    details: {
      main: '#555934',
    },
  },
});

export default theme;